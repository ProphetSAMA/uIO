server:
  port: 8080
#项目名称
spring:
  application:
    name: uIO
  profiles:
    active: dev
  banner:
    location: banner.txt
#数据库配置
  datasource:
    url: jdbc:mysql://wsssfun.rwlb.rds.aliyuncs.com:3306/springboot?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: wsssfun
    password: Qwe!230528
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
mybatis-plus:
  global-config:
    db-config:
      id-type: auto