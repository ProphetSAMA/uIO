<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fun.wsss.uio.mapper.DormitoryMapper">
    <select id="getAllDormitories" resultType="fun.wsss.uio.model.Dormitory">
        SELECT id FROM dormitory
    </select>
</mapper>
